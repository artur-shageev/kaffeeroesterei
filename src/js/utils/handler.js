

export const handlerHeader = ()=>{
    const header = document.querySelector('.container')
    header.classList.add('dark')
}

const handler = {
    handlerHeader: handlerHeader
}

export default handler